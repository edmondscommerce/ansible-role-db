---
percona_repo_url: "https://repo.percona.com/yum/percona-release-latest.noarch.rpm"
percona_repo_gpg_key_url: "https://www.percona.com/downloads/RPM-GPG-KEY-percona"
percona_version: 8.0
# TODO:  The names of the packages vary considerably, need to map the percona_version to the correct server package
percona_repo_package_server:
  - percona-server-server

percona_repo_package_extra:
  - MySQL-python
  - mytop

mysql_databases: []
#  - foo_db_name
#  - bar_db_name

mysql_users:
#  - name: username
#    host: "192.%"
#    password: "{{ username_mysql_pass }}"
#    grant: "ALL"
#    grant_db_tables: "{{ platform_db_name}}.*"
#  - name: username
#    host: "localhost"
#    password: "{{ username_mysql_pass }}"
#    grant: "ALL"
#    grant_db_tables: "{{ platform_db_name}}.*"
#  - name: username
#    host: "100.100.100.100"
#    password: "{{ username_mysql_pass }}"
#    grant: "ALL"
#    grant_db_tables: "{{ platform_db_name}}.*"